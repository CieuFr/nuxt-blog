import type { Config } from 'tailwindcss'

export default <Partial<Config>> {
    theme: {
        fontFamily: {
            'sans': ['Raleway', 'sans-serif'],
        },
        colors: {
            'white': '#FFFFFF',
            'blue': '#BBE1FA',
            'dark': '#1B262C',
        },
    }
}
